{
  "name": "superdyno",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "install": "yarn home && yarn home && yarn banking && yarn wallet && yarn expense && yarn profile",
    
    "host": "yarn --cwd host",
    "home": "yarn --cwd home",
    "banking": "yarn --cwd banking",
    "wallet": "yarn --cwd wallet",
    "expense": "yarn --cwd expense",
    "profile": "yarn --cwd profile"
  }
}
